{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "artifacts/agency-os",
    "artifacts/api-server",
    "artifacts/mockup-sandbox",
    "lib/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@workspace/api-zod && npm run build --workspace=@workspace/db && npm run build --workspace=@workspace/agency-os && npm run build --workspace=@workspace/api-server",
    "dev": "npm run build --workspace=@workspace/api-zod && npm run build --workspace=@workspace/db && npm run build --workspace=@workspace/agency-os && SERVE_STATIC=true PORT=3000 npm run dev --workspace=@workspace/api-server"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "drizzle-zod": "^0.8.3",
    "openai": "^6.45.0",
    "p-limit": "^7.3.0",
    "p-retry": "^7.1.1",
    "zod": "^4.4.3",
    "zod-validation-error": "^5.0.0"
  }
}
